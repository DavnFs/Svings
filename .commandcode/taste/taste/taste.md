# Taste
- Prefers responses written in English, and explicitly asks for English even when the surrounding material (e.g., an Indonesian-language plan) is in another language. Confidence: 0.9
- Explicitly asks for plan mode when requesting planning ("make sure to use plan mode") — wants a written plan laid out and approved before implementation begins. Confidence: 0.85
- When offered a choice of scope, picks the broadest option (e.g. build fix + dead-code prune + theme foundation) rather than the minimal "smallest reviewable diff" one, preferring bundled comprehensive changes over incremental steps. Confidence: 0.55
- Reports problems by pasting raw terminal/console error output with no accompanying instructions, expecting the agent to diagnose the root cause and apply the fix end-to-end (including environment/toolchain changes such as editing the user PATH or deleting and re-provisioning SDK components) before reporting back. Confidence: 0.7
- Expects the codebase and database schema to match the project's stated purpose, and calls out inherited/unrelated scope by asking "why is this here" — wants it deleted rather than kept "just in case" (e.g. removed the entire 10-table campus/academic domain from a money-tracking app's schema). Repeated, strongly-stated evidence. Confidence: 0.8
- Wants transactions captured automatically from bank/e-wallet notification emails (parsed by regex) rather than only hand-entered, and asks for the supporting schema/sync groundwork to be built. Confidence: 0.6
- Wants work verified against real infrastructure, not just unit tests or static analysis — explicitly asks for the live backend to be tested too (e.g. "test the API too supabase" against the actual project) and values end-to-end proof the change works. Repeated across sessions. Confidence: 0.65
- Prefers the agent to keep progressing on its own — nudges with terse "continue" messages and asks for the next features to be built in the same session rather than pausing for approval between steps. Repeated across multiple sessions, reaffirmed by "continue to do" / "continue finishing todos" nudges. Confidence: 0.75
- Avoids unnecessary dependencies, tools, and architectural layers — rejects proposals that add complexity without clear payoff (no Prisma over raw SQL migrations, no GoRouter for 8 screens, no Dio for 2 endpoints, no Clean Architecture domain layer for a CRUD app). Consistently asks "why do we need this" about anything that doesn't directly serve the immediate goal. Confidence: 0.65
- Wants the full verification gauntlet run before declaring something done — not just unit tests but also `flutter analyze`, API tests against the live backend, and a successful build. Treats "it compiles" as necessary but not sufficient proof. Confidence: 0.6
- Has chosen Supabase (cloud Postgres + Auth + PostgREST) as the backend and keeps building on it — did not pivot to a local-first embedded DB (Drift/Isar) even when the plan recommended it. Wants the cloud backend to be the source of truth, with schema migrations and RLS policies treated as first-class artifacts. Confidence: 0.7
- Wants proposals evaluated critically against the project's actual needs — expects the agent to push back and explain reasoning when a suggested tool/pattern doesn't fit (e.g. Prisma was rejected once its inability to express RLS and its migration-history fight with `supabase db push` was laid out). Also pushes back themselves when scope doesn't belong ("why there is role for student lectures"). Confidence: 0.65
- Accepts modern Material 3 UI patterns — frosted-glass / translucent bars, dynamic color (Wallpaper-derived theming), and a bottom NavigationBar over a drawer for primary nav. Suggested by the agent and adopted without pushback, then followed up on ("are the navigation bar already floating?"). Confidence: 0.55
- Wants every change committed to git — explicitly asks "git commit everychange" rather than leaving work uncommitted in the working tree. Confidence: 0.6
- Expects tests to be proven non-vacuous: after fixing a bug, deliberately reintroduce the fault (mutation check — swap the reactive wrapper back, flip the token to `noTransition`) to confirm the test fails for the right reason, then revert. Stated as an explicit requirement ("prove the test would catch a silently broken path") and repeated across tasks. Confidence: 0.7
- Wants an explicit call-site audit whenever a shared API, navigation path, or behavior changes — every caller enumerated and named in the report, not just the ones that needed edits. Confidence: 0.55
- Delivers work as numbered, multi-part requirement lists and expects each numbered item answered individually in the summary, including any deliberately skipped with the reasoning and a one-line path back if they disagree. Confidence: 0.55
- Wants UI changes verified visually: rendered screenshots written to a gitignored build output folder, listed by filename, and reviewed before the work is called done. Confidence: 0.55
- Prefers a custom in-app numeric keypad for money entry over the OS keyboard — a big live-formatted amount display pinned at the top and a 1-9/,/0/backspace pad pinned at the bottom with the form scrolling between. Confidence: 0.5
- Prefers bundled vector icon fonts (e.g. `simple_icons` brand marks) over platform emoji for app icons, and expects brand coverage to be checked one by one against the package source rather than assumed — absent brands fall back to a generic mark instead of a hand-drawn approximation. Confidence: 0.55
- In GetX-based Flutter code, expects reactive reads to happen inside the `Obx` closure itself — never wrapping a child widget or a plain value, both of which throw "improper use of a GetX"; the fix is to push the read into the closure or let the child own its own `Obx`. Confidence: 0.55
