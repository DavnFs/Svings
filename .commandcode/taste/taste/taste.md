# Taste
- Prefers responses written in English, and explicitly asks for English even when the surrounding material (e.g., an Indonesian-language plan) is in another language. Confidence: 0.9
- Explicitly asks for plan mode when requesting planning ("make sure to use plan mode") — wants a written plan laid out and approved before implementation begins. Confidence: 0.85
- When offered a choice of scope, picks the broadest option (e.g. build fix + dead-code prune + theme foundation) rather than the minimal "smallest reviewable diff" one, preferring bundled comprehensive changes over incremental steps. Confidence: 0.55
- Reports problems by pasting raw terminal/console error output with no accompanying instructions, expecting the agent to diagnose the root cause and apply the fix end-to-end (including environment/toolchain changes such as editing the user PATH or deleting and re-provisioning SDK components) before reporting back. Confidence: 0.6
- Expects the codebase and database schema to match the project's stated purpose, and calls out inherited/unrelated scope by asking "why is this here" — wants it deleted rather than kept "just in case" (e.g. removed the entire 10-table campus/academic domain from a money-tracking app's schema). Confidence: 0.7
- Wants transactions captured automatically from bank/e-wallet notification emails (parsed by regex) rather than only hand-entered, and asks for the supporting schema/sync groundwork to be built. Confidence: 0.6
- Wants work verified against real infrastructure, not just unit tests or static analysis — explicitly asks for the live backend to be tested too (e.g. "test the API too supabase" against the actual project) and values end-to-end proof the change works. Confidence: 0.55
- Prefers the agent to keep progressing on its own — nudges with terse "continue" messages and asks for the next features to be built in the same session rather than pausing for approval between steps. Confidence: 0.55
